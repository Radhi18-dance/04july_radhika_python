# Define a list of tuples
tuples_list = [(1, 2, 3), (4, 5, 6), (7, 8, 9)]

# Unzip the list of tuples using the zip(*tuples_list) expression
unzipped_list = list(zip(*tuples_list))

# Print the unzipped list
print(unzipped_list)  # Output: [(1, 4, 7), (2, 5, 8), (3, 6, 9)]

# Convert the tuples in the unzipped list to lists
unzipped_list = [list(t) for t in unzipped_list]

# Print the final unzipped list
print(unzipped_list)  # Output: [[1, 4, 7], [2, 5, 8], [3, 6, 9]]